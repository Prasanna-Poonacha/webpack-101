const css = require('./app.scss');

//console.log("Hello from app.js now and with dev server");

import React from 'react';
import ReactDOM from 'react-dom';

ReactDOM.render(
  <h1>Hello, world!</h1>,
  document.getElementById('root')
);